# Security

Report vulnerabilities privately to `security@gemacode.org`. Production must use Shopware/Symfony secrets or an environment secret, HTTPS, supervised workers, least privilege, redacted logs and tested rollback. The administration token field is intended only for demos.
